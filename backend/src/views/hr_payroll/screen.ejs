<div class="mx-auto w-full max-w-5xl space-y-6">
  <section class="rounded-3xl border border-slate-200 bg-white/95 p-6 shadow-lg">
    <div class="space-y-2">
      <h1 class="text-2xl font-display text-ink"><%= t(screen.titleKey) %></h1>
      <p class="text-sm text-muted"><%= screen.subtitle %></p>
    </div>
  </section>

  <section class="rounded-2xl border border-slate-200 bg-white/95 p-6 shadow-md">
    <div class="space-y-3 text-sm text-slate-700">
      <div class="flex flex-wrap gap-2">
        <span class="rounded-full bg-slate-100 px-3 py-1 font-semibold uppercase tracking-[0.14em] text-slate-600"><%= t("screen") %></span>
        <code class="rounded bg-slate-100 px-2 py-1 text-xs text-slate-700"><%= screen.routePath %></code>
      </div>
      <div class="flex flex-wrap gap-2">
        <span class="rounded-full bg-slate-100 px-3 py-1 font-semibold uppercase tracking-[0.14em] text-slate-600">Requirements Ref</span>
        <code class="rounded bg-slate-100 px-2 py-1 text-xs text-slate-700"><%= screen.requirementRef %></code>
      </div>
      <p class="text-slate-600">Implementation is now route-ready and permission-ready. CRUD and posting rules can be added incrementally without changing URL structure.</p>
    </div>
  </section>
</div>
